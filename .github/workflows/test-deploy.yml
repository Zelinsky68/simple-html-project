name: Test Connection

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Show Kubernetes Info
      run: |
        echo "Testing GitHub Actions..."
        echo "If you see this, workflow works!"
        echo "Next: add KUBECONFIG secret to deploy to Kubernetes"
